<UserControl x:Class="Hotel.Controls.NumericUpDown" 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Hotel.Controls"
             mc:Ignorable="d"
             d:DesignHeight="30" d:DesignWidth="120">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBox x:Name="txtValue" Grid.Column="0" Text="{Binding Value, RelativeSource={RelativeSource AncestorType=local:NumericUpDown}, UpdateSourceTrigger=PropertyChanged}" PreviewTextInput="TxtValue_PreviewTextInput" TextAlignment="Center" />
        <Button Grid.Column="1" Content="+" Click="BtnUp_Click" Width="20" Height="20" FontSize="10" Padding="0" Margin="2,0,0,0" />
        <Button Grid.Column="2" Content="-" Click="BtnDown_Click" Width="20" Height="20" FontSize="10" Padding="0" Margin="2,0,0,0" />
    </Grid>
</UserControl>