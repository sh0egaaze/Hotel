<Page x:Class="Hotel.Pages.ClientBookingDetailsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Hotel.Pages"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="800"
      Title="Детализация бронирования"
      Background="#121212">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,20">
            <Label x:Name="lblBookingInfo" Content="Информация о бронировании" FontSize="18" Foreground="White" HorizontalAlignment="Center"/>
            <Label x:Name="lblBookingDetails" Content="" Foreground="LightGray" HorizontalAlignment="Center" Margin="0,5,0,0"/>
        </StackPanel>

        <DataGrid x:Name="dgServices" Grid.Row="1" AutoGenerateColumns="False"
                  CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
                  HeadersVisibility="Column" GridLinesVisibility="All"
                  Background="#1E1E1E" Foreground="Black" BorderBrush="#3E3E42">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№" Binding="{Binding Index}" Width="40" />
                <DataGridTextColumn Header="Услуга/Номер" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Кол-во" Binding="{Binding Quantity}" Width="80" />
                <DataGridTextColumn Header="Ед. изм." Binding="{Binding Unit}" Width="80" />
                <DataGridTextColumn Header="Цена за ед." Binding="{Binding PricePerUnit, StringFormat='{}{0:N2} руб.'}" Width="100" />
                <DataGridTextColumn Header="Сумма" Binding="{Binding TotalLineAmount, StringFormat='{}{0:N2} руб.'}" Width="100" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Label Content="Итого:" FontSize="16" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <Label x:Name="lblTotalAmount" Content="0.00 руб." FontSize="16" Foreground="Yellow" FontWeight="Bold" VerticalAlignment="Center"/>
        </StackPanel>

        <Button x:Name="btnBack" Grid.Row="2" Content="Назад" Click="BtnBack_Click" Style="{StaticResource CustomButtonStyle}"
                HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="0,20,0,0"/>

    </Grid>
</Page>